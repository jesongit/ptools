spring:
  datasource:
    url: jdbc:mysql://home.jeson.im:23306/ptools?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: Jeson@123
  data:
    redis:
      host: localhost
      database: 0
      port: 6379
      password:
      timeout: 3000ms

springdoc:
  swagger-ui:
    path: /api
  writer-with-default-pretty-printer: true
  packages-to-scan: net.posase.ptools.modules.ums

logging:
  level:
    root: info
    com.macro.mall: debug