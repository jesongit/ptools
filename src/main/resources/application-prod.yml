spring:
  datasource:
    url: jdbc:mysql://home.jeson.im:23306/ptools?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: Jeson@123
  data:
    redis:
      host: localhost
      database: 0
      port: 6379
      password:
      timeout: 3000ms

logging:
  file:
    path: /var/logs
  level:
    root: info
    net.posase.ptools: info
